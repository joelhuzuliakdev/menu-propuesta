---
import Layout from "../layouts/Layout.astro";
import { products, categories } from "../data/products";

console.log("Productos cargados en Astro:", products?.length);
---

<Layout title="Fast Food - Delivery">
  <main class="min-h-screen bg-gradient-to-b from-zinc-950 via-zinc-900 to-zinc-950">
    <section class="container mx-auto px-4 py-8">
      <!-- HEADER / NAV CON LOGO + BOTÓN CARRITO -->
      <section class="flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4 mb-8">
        <div class="flex items-center gap-3">
          <div
            class="h-12 w-12 rounded-full bg-amber-400 flex items-center justify-center text-zinc-950 text-xl font-black shadow-lg shadow-amber-500/40"
          >
            GM
          </div>
          <div class="flex flex-col leading-tight">
            <h1 class="text-2xl font-semibold text-zinc-50">
              Grill Master
            </h1>
            <p class="text-sm text-zinc-400">
              Hamburguesas, pizzas, lomitos y más 🍔
            </p>
          </div>
        </div>

        <button
          id="btn-open-cart"
          class="inline-flex items-center gap-2 rounded-full border border-amber-400 bg-zinc-900 px-4 py-2 text-sm font-medium text-amber-300 hover:bg-amber-400 hover:text-zinc-950 transition shadow-md shadow-amber-500/30"
          type="button"
          aria-label="Abrir carrito de compras"
        >
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0z"></path>
          </svg>
          <span>Carrito</span>
          <span
            id="cart-count"
            class="text-xs bg-amber-500 text-zinc-950 rounded-full px-2 py-[2px] font-bold min-w-[20px] text-center"
          >
            0
          </span>
        </button>
      </section>

      <!-- FILTROS -->
      <section
        id="filters"
        class="flex flex-wrap gap-2 justify-center mb-8 text-sm"
      >
        {
          categories.map((cat) => (
            <button
              type="button"
              data-filter={cat.id}
              class={`filter-btn px-4 py-2 rounded-full border text-xs uppercase tracking-wide transition ${
                cat.id === "todos"
                  ? "bg-amber-400 border-amber-400 text-zinc-950 font-semibold shadow shadow-amber-500/40"
                  : "bg-zinc-900 border-zinc-700 text-zinc-300 hover:bg-zinc-800"
              }`}
              aria-label={`Filtrar por ${cat.name}`}
            >
              {cat.name}
            </button>
          ))
        }
      </section>

      <!-- CONTADOR DE PRODUCTOS -->
      <p class="text-xs text-zinc-400 mb-4 text-center">
        Mostrando <span id="products-count">{products.length}</span> productos
      </p>

      <!-- GRID DE PRODUCTOS -->
      <section
        id="products-grid"
        class="grid gap-6 grid-cols-1 sm:grid-cols-2 lg:grid-cols-3"
      >
        {
          products.map((product) => (
            <article
              class="product-card bg-zinc-900/90 border border-zinc-800 rounded-2xl overflow-hidden flex flex-col shadow-lg shadow-black/50 hover:-translate-y-1 hover:shadow-2xl hover:border-zinc-700 transition-all duration-300"
              data-category={product.category}
              data-id={product.id}
              data-price={product.price}
              style="animation: fadeIn 0.5s ease-out"
            >
              <div class="h-56 w-full bg-zinc-800 overflow-hidden relative">
                <img
                  src={product.image}
                  alt={`${product.name} - ${product.description}`}
                  class="h-full w-full object-cover hover:scale-105 transition-transform duration-500 product-image"
                  loading="lazy"
                />
              </div>

              <div class="p-4 flex flex-col flex-1">
                <div class="flex items-start justify-between gap-2 mb-2">
                  <h3 class="font-semibold text-zinc-50 text-sm product-name">
                    {product.name}
                  </h3>
                  <span
                    class="text-amber-400 font-bold text-sm whitespace-nowrap product-price"
                  >
                    ${product.price.toLocaleString("es-AR")}
                  </span>
                </div>

                <p class="text-xs text-zinc-400 mb-3 line-clamp-2 product-description">
                  {product.description}
                </p>

                <div class="mt-auto flex gap-2">
                  <button
                    type="button"
                    class="btn-customize flex-1 rounded-full border border-zinc-600 px-3 py-2 text-[11px] font-medium text-zinc-200 hover:bg-zinc-800 hover:border-zinc-500 transition"
                    data-product-id={product.id}
                    aria-label={`Personalizar ${product.name}`}
                  >
                    Personalizar
                  </button>
                  <button
                    type="button"
                    class="btn-add flex-1 rounded-full bg-amber-400 px-3 py-2 text-[11px] font-semibold text-zinc-950 hover:bg-amber-300 transition active:scale-95"
                    data-product-id={product.id}
                    aria-label={`Agregar ${product.name} al carrito`}
                  >
                    Agregar
                  </button>
                </div>
              </div>
            </article>
          ))
        }
      </section>
    </section>

    <!-- MODAL DEL CARRITO -->
    <div
      id="cart-modal"
      class="fixed inset-0 bg-black/80 backdrop-blur-sm z-50 hidden items-center justify-center p-4"
      role="dialog"
      aria-modal="true"
      aria-labelledby="cart-title"
    >
      <div class="bg-zinc-900 border border-zinc-800 rounded-2xl max-w-md w-full max-h-[90vh] overflow-hidden shadow-2xl animate-slide-up">
        <!-- Header del modal -->
        <div class="flex items-center justify-between p-6 border-b border-zinc-800">
          <h2 id="cart-title" class="text-xl font-semibold text-zinc-50">Tu Pedido</h2>
          <button
            id="btn-close-cart"
            class="text-zinc-400 hover:text-zinc-200 transition"
            type="button"
            aria-label="Cerrar carrito"
          >
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </button>
        </div>

        <!-- Contenido del carrito -->
        <div id="cart-items" class="p-6 overflow-y-auto max-h-[50vh]">
          <p class="text-zinc-400 text-sm text-center py-8">
            Tu carrito está vacío
          </p>
        </div>

        <!-- Footer con total y botón WhatsApp -->
        <div class="p-6 border-t border-zinc-800 bg-zinc-950/50">
          <div class="flex items-center justify-between mb-4">
            <span class="text-zinc-400 text-sm">Total:</span>
            <span id="cart-total" class="text-2xl font-bold text-amber-400">$0</span>
          </div>
          <button
            id="btn-whatsapp"
            class="w-full rounded-full bg-green-600 hover:bg-green-500 px-6 py-3 text-sm font-semibold text-white transition flex items-center justify-center gap-2 active:scale-95 disabled:opacity-50 disabled:cursor-not-allowed"
            type="button"
            disabled
          >
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
              <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413Z"></path>
            </svg>
            Pedir por WhatsApp
          </button>
        </div>
      </div>
    </div>

    <!-- MODAL DE PERSONALIZACIÓN -->
    <div
      id="customize-modal"
      class="fixed inset-0 bg-black/80 backdrop-blur-sm z-50 hidden items-center justify-center p-4"
      role="dialog"
      aria-modal="true"
    >
      <div class="bg-zinc-900 border border-zinc-800 rounded-2xl max-w-md w-full max-h-[90vh] overflow-hidden shadow-2xl">
        <div class="flex items-center justify-between p-6 border-b border-zinc-800">
          <h2 class="text-xl font-semibold text-zinc-50">Personalizar producto</h2>
          <button
            id="btn-close-customize"
            class="text-zinc-400 hover:text-zinc-200 transition"
            type="button"
          >
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </button>
        </div>

        <div class="p-6 overflow-y-auto max-h-[60vh]">
          <div id="customize-content">
            <!-- Quitar ingredientes -->
            <div class="mb-4">
              <h3 class="text-sm font-semibold text-zinc-100 mb-2">
                ¿Querés sacar algo?
              </h3>
              <p class="text-xs text-zinc-400 mb-2">
                Tildá lo que NO querés que lleve tu pedido:
              </p>
              <div class="flex flex-wrap gap-2 text-xs text-zinc-200">
                <label class="inline-flex items-center gap-1 bg-zinc-800 rounded-full px-3 py-1 cursor-pointer">
                  <input type="checkbox" class="opt-remove" value="sin queso" />
                  <span>Sin queso</span>
                </label>
                <label class="inline-flex items-center gap-1 bg-zinc-800 rounded-full px-3 py-1 cursor-pointer">
                  <input type="checkbox" class="opt-remove" value="sin cebolla" />
                  <span>Sin cebolla</span>
                </label>
                <label class="inline-flex items-center gap-1 bg-zinc-800 rounded-full px-3 py-1 cursor-pointer">
                  <input type="checkbox" class="opt-remove" value="sin lechuga" />
                  <span>Sin lechuga</span>
                </label>
                <label class="inline-flex items-center gap-1 bg-zinc-800 rounded-full px-3 py-1 cursor-pointer">
                  <input type="checkbox" class="opt-remove" value="sin tomate" />
                  <span>Sin tomate</span>
                </label>
                <label class="inline-flex items-center gap-1 bg-zinc-800 rounded-full px-3 py-1 cursor-pointer">
                  <input type="checkbox" class="opt-remove" value="sin aderezos" />
                  <span>Sin aderezos</span>
                </label>
              </div>
            </div>

            <!-- Extras -->
            <div class="mb-4">
              <h3 class="text-sm font-semibold text-zinc-100 mb-2">
                Extras (se suman al precio):
              </h3>
              <p class="text-xs text-zinc-400 mb-2">
                Elegí si querés agregar algo extra a tu pedido. El importe se suma al precio base.
              </p>
              <div class="flex flex-wrap gap-2 text-xs text-zinc-200">
                <label class="inline-flex items-center gap-1 bg-zinc-800 rounded-full px-3 py-1 cursor-pointer">
                  <input type="checkbox" class="opt-extra" data-extra="3000" value="extra queso" />
                  <span>Extra queso (+$3.000)</span>
                </label>
                <label class="inline-flex items-center gap-1 bg-zinc-800 rounded-full px-3 py-1 cursor-pointer">
                  <input type="checkbox" class="opt-extra" data-extra="3000" value="extra bacon" />
                  <span>Extra bacon (+$3.000)</span>
                </label>
                <label class="inline-flex items-center gap-1 bg-zinc-800 rounded-full px-3 py-1 cursor-pointer">
                  <input type="checkbox" class="opt-extra" data-extra="3000" value="carne extra" />
                  <span>Carne extra (+$3.000)</span>
                </label>
              </div>
            </div>

            <!-- Notas libres -->
            <div class="mb-4">
              <label class="block text-sm font-medium text-zinc-300 mb-2">
                Notas especiales:
              </label>
              <textarea
                id="custom-notes"
                class="w-full bg-zinc-800 border border-zinc-700 rounded-lg px-3 py-2 text-sm text-zinc-200 focus:outline-none focus:ring-2 focus:ring-amber-400"
                rows="3"
                placeholder="Ej: bien cocida, sin sal, cortar a la mitad..."
              ></textarea>
            </div>
          </div>
        </div>

        <div class="p-6 border-t border-zinc-800">
          <button
            id="btn-add-customized"
            class="w-full rounded-full bg-amber-400 hover:bg-amber-300 px-6 py-3 text-sm font-semibold text-zinc-950 transition active:scale-95"
            type="button"
          >
            Agregar al carrito
          </button>
        </div>
      </div>
    </div>

    <!-- MODAL DE CONFIRMACIÓN ANTES DE AGREGAR -->
    <div
      id="confirm-add-modal"
      class="fixed inset-0 bg-black/80 backdrop-blur-sm z-50 hidden items-center justify-center p-4"
      role="dialog"
      aria-modal="true"
    >
      <div class="bg-zinc-900 border border-zinc-800 rounded-2xl max-w-md w-full shadow-2xl animate-slide-up">
        <div class="p-6">
          <h2 class="text-lg font-semibold text-zinc-50 mb-2">
            ¿Querés personalizar tu pedido antes de agregarlo?
          </h2>
          <p class="text-sm text-zinc-300 mb-4">
            Podés sacar ingredientes (sin lechuga, sin salsas, etc.) o sumar extras como queso, bacon o carne extra.
          </p>
          <div class="flex justify-end gap-3">
            <button
              id="btn-confirm-no"
              type="button"
              class="px-4 py-2 rounded-full text-sm bg-zinc-800 text-zinc-200 hover:bg-zinc-700 transition"
            >
              Agregar así
            </button>
            <button
              id="btn-confirm-yes"
              type="button"
              class="px-4 py-2 rounded-full text-sm bg-amber-400 text-zinc-950 font-semibold hover:bg-amber-300 transition"
            >
              Personalizar pedido
            </button>
          </div>
        </div>
      </div>
    </div>
  </main>

  <!-- Animaciones simples usadas en la UI -->
  <style>
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(6px); }
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes slideUp {
      from { transform: translateY(12px); opacity: 0; }
      to { transform: translateY(0); opacity: 1; }
    }
    .animate-slide-up { animation: slideUp 0.25s ease-out; }
  </style>

  <!-- LÓGICA DEL CARRITO -->
  <script>
    // DATOS DEL CARRITO (en memoria)
    let cart = [];
    let currentCustomizingProduct = null;
    let pendingProductId = null; // para el modal de confirmación

    // NÚMERO DE WHATSAPP (cambiar por el tuyo)
    const WHATSAPP_NUMBER = "3572548014"; // Formato: código país + número

    // Elementos del DOM
    const btnOpenCart = document.getElementById("btn-open-cart");
    const btnCloseCart = document.getElementById("btn-close-cart");
    const cartModal = document.getElementById("cart-modal");
    const cartItems = document.getElementById("cart-items");
    const cartTotal = document.getElementById("cart-total");
    const cartCount = document.getElementById("cart-count");
    const btnWhatsApp = document.getElementById("btn-whatsapp");
    
    const customizeModal = document.getElementById("customize-modal");
    const btnCloseCustomize = document.getElementById("btn-close-customize");
    const customNotes = document.getElementById("custom-notes");
    const btnAddCustomized = document.getElementById("btn-add-customized");

    const confirmModal = document.getElementById("confirm-add-modal");
    const btnConfirmNo = document.getElementById("btn-confirm-no");
    const btnConfirmYes = document.getElementById("btn-confirm-yes");

    // Opciones de quitar y extras
    const removeOptions = document.querySelectorAll(".opt-remove");
    const extraOptions = document.querySelectorAll(".opt-extra");

    // SERIALIZAR PRODUCTOS DESDE EL DOM
    const productsData = Array.from(document.querySelectorAll(".product-card")).map(card => {
      const id = card.dataset.id;
      const name = card.querySelector(".product-name")?.textContent?.trim() || "";
      const price = parseFloat(card.dataset.price || "0");
      const image = card.querySelector(".product-image")?.getAttribute("src") || "";
      const description = card.querySelector(".product-description")?.textContent?.trim() || "";
      const category = card.dataset.category || "todos";
      return { id, name, price, image, description, category };
    });

    // ============ FUNCIONES DE CARRITO ============

    function addToCart(productId, optionsOrNotes) {
      const product = productsData.find((p) => p.id === String(productId));
      if (!product) {
        console.warn("Producto no encontrado para agregar al carrito:", productId);
        return;
      }

      let notes = "";
      let extraAmount = 0; // monto extra en pesos

      if (typeof optionsOrNotes === "string") {
        notes = optionsOrNotes;
      } else if (typeof optionsOrNotes === "object" && optionsOrNotes !== null) {
        notes = optionsOrNotes.notes || "";
        extraAmount = optionsOrNotes.extraAmount || 0;
      }

      const finalPrice = product.price + extraAmount;

      const existingItem = cart.find(
        (item) =>
          item.id === productId &&
          item.notes === notes &&
          (item.extraAmount || 0) === extraAmount
      );

      if (existingItem) {
        existingItem.quantity++;
      } else {
        cart.push({
          ...product,
          price: finalPrice,
          basePrice: product.price,
          extraAmount,
          quantity: 1,
          notes,
        });
      }

      updateCart();
      showNotification(`Se agregó "${product.name}" al carrito`);
    }

    function removeFromCart(index) {
      cart.splice(index, 1);
      updateCart();
    }

    function updateQuantity(index, delta) {
      cart[index].quantity += delta;
      if (cart[index].quantity <= 0) {
        removeFromCart(index);
      } else {
        updateCart();
      }
    }

    function calculateTotal() {
      return cart.reduce((sum, item) => sum + (item.price * item.quantity), 0);
    }

    function updateCart() {
      // Actualizar contador
      const totalItems = cart.reduce((sum, item) => sum + item.quantity, 0);
      cartCount.textContent = totalItems;

      // Actualizar contenido del carrito
      if (cart.length === 0) {
        cartItems.innerHTML = `
          <p class="text-zinc-400 text-sm text-center py-8">
            Tu carrito está vacío
          </p>
        `;
        btnWhatsApp.disabled = true;
      } else {
        cartItems.innerHTML = cart.map((item, index) => `
          <div class="flex gap-3 mb-4 pb-4 border-b border-zinc-800 last:border-0">
            <img 
              src="${item.image}" 
              alt="${item.name}"
              class="w-16 h-16 rounded-lg object-cover bg-zinc-800"
              loading="lazy"
            />
            <div class="flex-1">
              <div class="flex items-start justify-between">
                <h3 class="text-sm font-medium text-zinc-50">${item.name}</h3>
                <p class="text-xs text-zinc-400">$${item.price.toLocaleString("es-AR")}</p>
              </div>
              ${item.notes ? `<p class="text-xs text-amber-400 italic mt-1">${item.notes}</p>` : ""}
              
              <div class="flex items-center gap-2 mt-2">
                <button 
                  class="btn-decrease w-6 h-6 rounded bg-zinc-800 hover:bg-zinc-700 text-zinc-300 text-sm transition"
                  data-index="${index}"
                  aria-label="Disminuir cantidad"
                >
                  −
                </button>
                <span class="text-sm text-zinc-300 min-w-[20px] text-center">${item.quantity}</span>
                <button 
                  class="btn-increase w-6 h-6 rounded bg-zinc-800 hover:bg-zinc-700 text-zinc-300 text-sm transition"
                  data-index="${index}"
                  aria-label="Aumentar cantidad"
                >
                  +
                </button>
                <button 
                  class="btn-remove ml-auto text-xs text-red-400 hover:text-red-300 transition"
                  data-index="${index}"
                  aria-label="Eliminar del carrito"
                >
                  Eliminar
                </button>
              </div>
            </div>
          </div>
        `).join("");

        // Event listeners para botones del carrito
        document.querySelectorAll(".btn-decrease").forEach(btn => {
          btn.addEventListener("click", () => {
            updateQuantity(parseInt(btn.dataset.index), -1);
          });
        });

        document.querySelectorAll(".btn-increase").forEach(btn => {
          btn.addEventListener("click", () => {
            updateQuantity(parseInt(btn.dataset.index), 1);
          });
        });

        document.querySelectorAll(".btn-remove").forEach(btn => {
          btn.addEventListener("click", () => {
            removeFromCart(parseInt(btn.dataset.index));
          });
        });

        btnWhatsApp.disabled = false;
      }

      // Actualizar total
      cartTotal.textContent = `$${calculateTotal().toLocaleString("es-AR")}`;
    }

    // ============ MODAL HANDLERS ============
    function openModal(modal) {
      modal.classList.remove("hidden");
      modal.classList.add("flex");
      document.body.style.overflow = "hidden";
    }

    function closeModal(modal) {
      modal.classList.add("hidden");
      modal.classList.remove("flex");
      document.body.style.overflow = "";
    }

    btnOpenCart?.addEventListener("click", () => openModal(cartModal));
    btnCloseCart?.addEventListener("click", () => closeModal(cartModal));
    cartModal?.addEventListener("click", (e) => {
      if (e.target === cartModal) closeModal(cartModal);
    });

    btnCloseCustomize?.addEventListener("click", () => closeModal(customizeModal));
    customizeModal?.addEventListener("click", (e) => {
      if (e.target === customizeModal) closeModal(customizeModal);
    });

    // Confirm modal backdrop
    confirmModal?.addEventListener("click", (e) => {
      if (e.target === confirmModal) closeModal(confirmModal);
    });

    // ============ WHATSAPP ============
    btnWhatsApp?.addEventListener("click", () => {
      if (cart.length === 0) return;

      let mensaje = "¡Hola! Quiero hacer el siguiente pedido:%0A%0A";
      
      cart.forEach(item => {
        const subtotal = item.price * item.quantity;
        mensaje += `🍔 ${item.quantity}x ${item.name} - $${subtotal.toLocaleString("es-AR")}%0A`;
        if (item.notes) {
          mensaje += `   ⚠️ ${item.notes}%0A`;
        }
      });

      mensaje += `%0A💰 *Total: $${calculateTotal().toLocaleString("es-AR")}*`;

      const url = `https://wa.me/${WHATSAPP_NUMBER}?text=${mensaje}`;
      window.open(url, "_blank");
    });

    // ============ BOTONES DE PRODUCTOS ============

    // Abrir modal de confirmación antes de agregar
    document.querySelectorAll(".btn-add").forEach(btn => {
      btn.addEventListener("click", () => {
        pendingProductId = btn.dataset.productId;
        openModal(confirmModal);
      });
    });

    // Personalizar directo
    document.querySelectorAll(".btn-customize").forEach(btn => {
      btn.addEventListener("click", () => {
        currentCustomizingProduct = btn.dataset.productId;
        // resetear opciones
        removeOptions.forEach(o => o.checked = false);
        extraOptions.forEach(o => o.checked = false);
        customNotes.value = "";
        openModal(customizeModal);
      });
    });

    // Confirmación: Agregar sin personalizar
    btnConfirmNo?.addEventListener("click", () => {
      if (!pendingProductId) {
        closeModal(confirmModal);
        return;
      }
      addToCart(pendingProductId);
      pendingProductId = null;
      closeModal(confirmModal);
    });

    // Confirmación: Quiero personalizar
    btnConfirmYes?.addEventListener("click", () => {
      if (!pendingProductId) {
        closeModal(confirmModal);
        return;
      }
      currentCustomizingProduct = pendingProductId;
      // resetear opciones
      removeOptions.forEach(o => o.checked = false);
      extraOptions.forEach(o => o.checked = false);
      customNotes.value = "";
      openModal(customizeModal);
      pendingProductId = null;
      closeModal(confirmModal);
    });

    // Guardar producto personalizado
    btnAddCustomized?.addEventListener("click", () => {
      if (!currentCustomizingProduct) return;

      const notesParts = [];

      // ingredientes que se sacan
      removeOptions.forEach(opt => {
        if (opt.checked) notesParts.push(opt.value);
      });

      // extras en pesos
      let extraAmount = 0;
      extraOptions.forEach(opt => {
        if (opt.checked) {
          const extra = parseFloat(opt.dataset.extra || "0");
          extraAmount += extra;
          notesParts.push(`${opt.value} (+$${extra.toLocaleString("es-AR")})`);
        }
      });

      // notas libres
      if (customNotes.value.trim()) {
        notesParts.push(customNotes.value.trim());
      }

      const notes = notesParts.join(" | ");

      addToCart(currentCustomizingProduct, { notes, extraAmount });

      closeModal(customizeModal);
      currentCustomizingProduct = null;
    });

    // ============ FILTROS ============
    let activeFilter = "todos";
    const filterButtons = document.querySelectorAll(".filter-btn");
    const cards = document.querySelectorAll(".product-card");
    const productsCount = document.getElementById("products-count");

    function aplicarFiltro() {
      let visibleCount = 0;
      
      cards.forEach((card) => {
        const cardCategory = card.dataset.category;
        const shouldShow = activeFilter === "todos" || cardCategory === activeFilter;
        
        if (shouldShow) {
          card.classList.remove("hidden");
          card.style.animation = "fadeIn 0.4s ease-out";
          visibleCount++;
        } else {
          card.classList.add("hidden");
        }
      });

      if (productsCount) {
        productsCount.textContent = visibleCount;
      }
    }

    filterButtons.forEach((btn) => {
      btn.addEventListener("click", () => {
        activeFilter = btn.dataset.filter;

        filterButtons.forEach((b) => {
          b.classList.remove(
            "bg-amber-400",
            "border-amber-400",
            "text-zinc-950",
            "font-semibold",
            "shadow",
            "shadow-amber-500/40"
          );
          b.classList.add("bg-zinc-900", "border-zinc-700", "text-zinc-300");
        });

        btn.classList.remove("bg-zinc-900", "border-zinc-700", "text-zinc-300");
        btn.classList.add(
          "bg-amber-400",
          "border-amber-400",
          "text-zinc-950",
          "font-semibold",
          "shadow",
          "shadow-amber-500/40"
        );

        aplicarFiltro();
      });
    });

    aplicarFiltro();

    // ============ NOTIFICACIONES ============
    function showNotification(message) {
      const notification = document.createElement("div");
      notification.className = "fixed bottom-4 right-4 bg-green-600 text-white px-6 py-3 rounded-lg shadow-lg z-50 animate-slide-up";
      notification.textContent = message;
      document.body.appendChild(notification);

      setTimeout(() => {
        notification.style.opacity = "0";
        notification.style.transform = "translateY(20px)";
        notification.style.transition = "all 0.3s ease-out";
        setTimeout(() => notification.remove(), 300);
      }, 2000);
    }

    console.log("🍔 Sistema de carrito inicializado", { productsData });
  </script>
</Layout>
